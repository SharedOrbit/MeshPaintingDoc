import{_ as a,o as i,c as t,a3 as n}from"./chunks/framework.CWCEHGF4.js";const u=JSON.parse('{"title":"Troubleshooting","description":"","frontmatter":{},"headers":[],"relativePath":"reference/troubleshooting.md","filePath":"reference/troubleshooting.md","lastUpdated":1784814605000}'),o={name:"reference/troubleshooting.md"};function s(l,e,r,h,c,d){return i(),t("div",null,[...e[0]||(e[0]=[n(`<h1 id="troubleshooting" tabindex="-1">Troubleshooting <a class="header-anchor" href="#troubleshooting" aria-label="Permalink to &quot;Troubleshooting&quot;">​</a></h1><p>Runtime Mesh Painting writes paint into a render target through mesh UVs. Most issues come from one of these areas:</p><ul><li>Target component setup</li><li>Material setup</li><li>Collision or trace setup</li><li>The mesh UV layout</li></ul><p>If the plugin works on a simple cube but fails on a production asset, treat it as an asset, UV, material slot, LOD, or collision issue first.</p><h2 id="start-here" tabindex="-1">Start Here <a class="header-anchor" href="#start-here" aria-label="Permalink to &quot;Start Here&quot;">​</a></h2><p>Check these before changing advanced settings:</p><ul><li>The actor has <code>RuntimeMeshPaintTargetComponent</code>.</li><li>The player has <code>PaintingModeControllerComponent</code>.</li><li>The mesh material uses the <code>Mesh Painting</code> material function.</li><li>The material function outputs are connected to the material output.</li><li><code>Brush Emissive</code> is connected to <code>Emissive Color</code> if you expect the on-mesh brush preview to be visible.</li><li>Material <code>UV Index</code> matches <code>RuntimeMeshPaintTargetComponent.UVChannel</code>.</li><li>The mesh blocks the controller <code>Paint Trace Channel</code>, usually <code>Visibility</code>.</li><li>The correct mesh component and material section are being hit.</li><li><code>Clip Brush to UVIsland</code> is enabled unless cross-island painting is intentional.</li></ul><h2 id="paint-does-not-appear-at-all" tabindex="-1">Paint Does Not Appear At All <a class="header-anchor" href="#paint-does-not-appear-at-all" aria-label="Permalink to &quot;Paint Does Not Appear At All&quot;">​</a></h2><p>If nothing appears, check the basic chain:</p><div class="language-text vp-adaptive-theme"><button title="Copy Code" class="copy"></button><span class="lang">text</span><pre class="shiki shiki-themes github-light github-dark vp-code" tabindex="0"><code><span class="line"><span>Controller trace</span></span>
<span class="line"><span>-&gt; Paint target component</span></span>
<span class="line"><span>-&gt; Runtime render target</span></span>
<span class="line"><span>-&gt; Mesh Painting material function</span></span>
<span class="line"><span>-&gt; Material output</span></span></code></pre></div><p>Common causes:</p><ul><li>Missing or uninitialized <code>RuntimeMeshPaintTargetComponent</code>.</li><li>The controller target filter excludes the target.</li><li><code>Mesh Targets</code> points to the wrong component.</li><li>The mesh material does not contain the <code>Mesh Painting</code> function.</li><li>The material slot you are hitting uses a different material.</li><li><code>UVChannel</code> and material <code>UV Index</code> do not match.</li><li>The mesh does not block the paint trace channel.</li><li>The material only shows the original texture because the paint outputs are not connected to the final material outputs.</li></ul><h2 id="brush-preview-appears-but-paint-does-not-apply" tabindex="-1">Brush Preview Appears, But Paint Does Not Apply <a class="header-anchor" href="#brush-preview-appears-but-paint-does-not-apply" aria-label="Permalink to &quot;Brush Preview Appears, But Paint Does Not Apply&quot;">​</a></h2><p>If the brush preview follows the surface, tracing is usually working. In this case, the most common cause is material setup.</p><p>Check:</p><ul><li>The mesh material uses the <code>Mesh Painting</code> material function.</li><li>The function outputs are connected to <code>Base Color</code>, <code>Metallic</code>, <code>Roughness</code>, and <code>Emissive Color</code> as needed.</li><li>The material is assigned to the mesh section you are painting.</li><li>The runtime paint texture parameter names are still compatible with the plugin material function.</li><li>The target render targets were created successfully.</li></ul><p>Fix the material first, then check the paint target and UV channel.</p><h2 id="brush-preview-missing-or-flickering" tabindex="-1">Brush Preview Missing Or Flickering <a class="header-anchor" href="#brush-preview-missing-or-flickering" aria-label="Permalink to &quot;Brush Preview Missing Or Flickering&quot;">​</a></h2><p>This is usually a trace, material preview, collision, or target filtering issue.</p><p>Check:</p><ul><li><code>Enable Brush Area Preview</code> is enabled on <code>PaintingModeControllerComponent</code>.</li><li><code>Brush Emissive</code> from the <code>Mesh Painting</code> material function is connected to <code>Emissive Color</code>.</li><li>The material still receives the default <code>BrushPreviewMaskTexture</code> parameter from the paint target component.</li><li>The mesh blocks <code>Paint Trace Channel</code>.</li><li><code>Paint Trace Complex</code> is enabled when simple collision is too rough.</li><li>Another object is not blocking the trace before the paintable mesh.</li><li>The cursor is not over the color picker UI.</li><li>Eyedropper mode is not active.</li><li><code>Control Mode</code> is not <code>None</code> unless you drive painting manually.</li><li>The target is not excluded by <code>Paint Target Components</code>.</li></ul><p>For skeletal meshes, per-poly collision is not required, but the physics or collision setup still needs to produce hits near the visible surface.</p><h2 id="paint-appears-in-the-wrong-place" tabindex="-1">Paint Appears In The Wrong Place <a class="header-anchor" href="#paint-appears-in-the-wrong-place" aria-label="Permalink to &quot;Paint Appears In The Wrong Place&quot;">​</a></h2><p>Wrong-location paint is usually a UV mismatch or a broken paint UV layout.</p><p>Check:</p><ul><li><code>RuntimeMeshPaintTargetComponent.UVChannel</code> and material <code>UV Index</code> are the same.</li><li>The selected UV channel exists on the mesh.</li><li>The selected paint UV channel is unwrapped correctly.</li><li>The paint UVs are inside the expected 0-1 space.</li><li>The UVs are not collapsed, zero-area, stacked unintentionally, or assigned to the wrong channel.</li><li>The material is not using another <code>TextureCoordinate</code> path after the paint function.</li><li>LODs use compatible paint UV layouts.</li><li>The mesh does not rely on unusual negative or non-uniform scale.</li></ul><p>If changing <code>UVChannel</code> fixes the issue, the plugin was painting correctly but the material or asset was reading another UV channel.</p><p>If the model UVs are broken, the plugin cannot solve that in code. Runtime Mesh Painting can only paint through the UV data the model provides, so repairing invalid UVs is the responsibility of the asset/model setup.</p><p>If the paint appears on a mirrored or duplicated part of the model, also check <a href="#mirrored-or-overlapped-uvs">Mirrored Or Overlapped UVs</a>.</p><h2 id="uv-islands-seams-and-large-brushes" tabindex="-1">UV Islands, Seams, And Large Brushes <a class="header-anchor" href="#uv-islands-seams-and-large-brushes" aria-label="Permalink to &quot;UV Islands, Seams, And Large Brushes&quot;">​</a></h2><p>Large brushes can reach outside the hit UV island in texture space. <code>Clip Brush to UVIsland</code> prevents unrelated UV islands from being painted.</p><p>Keep it enabled for:</p><ul><li>Characters with many UV islands.</li><li>Cubes or hard-surface meshes with separated faces.</li><li>Large brush sizes.</li><li>Visible seam areas.</li></ul><p>Visible seams usually happen because adjacent world-space surfaces are separate UV islands. The brush is clipped to the hit island, so the other side of the seam may need its own stroke.</p><p>To reduce seam visibility:</p><ul><li>Use a dedicated paint UV channel with cleaner island placement.</li><li>Give islands enough padding.</li><li>Increase render target resolution when islands are too small.</li><li>Keep seams away from highly visible areas when possible.</li><li>Paint across both sides of the seam when the UVs are split.</li></ul><p>A UV-space paint system cannot make two separated UV islands behave exactly like one continuous surface unless both islands receive paint.</p><h2 id="mirrored-or-overlapped-uvs" tabindex="-1">Mirrored Or Overlapped UVs <a class="header-anchor" href="#mirrored-or-overlapped-uvs" aria-label="Permalink to &quot;Mirrored Or Overlapped UVs&quot;">​</a></h2><p>Mirrored and overlapped UVs are valid only when shared paint is acceptable.</p><p>If two surfaces share the same UV texels, they share the same paint pixels:</p><ul><li>Painting one side can paint the mirrored side.</li><li>Erasing one side can erase the other.</li><li>Both surfaces can sample the same color.</li><li>They cannot be painted independently on that UV channel.</li></ul><p>This is expected for UV-space painting. If each side must be painted independently, create a unique paint UV channel and set both <code>RuntimeMeshPaintTargetComponent.UVChannel</code> and material <code>UV Index</code> to that channel.</p><p>You can still keep mirrored UVs for normal textures. The paint UV channel can be separate.</p><h2 id="brush-size-looks-different-across-meshes" tabindex="-1">Brush Size Looks Different Across Meshes <a class="header-anchor" href="#brush-size-looks-different-across-meshes" aria-label="Permalink to &quot;Brush Size Looks Different Across Meshes&quot;">​</a></h2><p>The brush is projected by the GPU and written into the mesh paint UV render target. Even when the preview and paint use the same projection path, the visible footprint can still look inconsistent when the asset UV density is inconsistent.</p><p>Brush size can look inconsistent when:</p><ul><li>Two meshes have different paint UV texel density.</li><li>A surface uses a very small UV island.</li><li>Object scale is not applied.</li><li>Render target resolution is too low.</li><li>The material reads a different UV channel than the target component.</li></ul><p>Use consistent texel density, apply object scale before export, and use a dedicated paint UV channel for assets that need predictable brush size.</p><h2 id="triangle-lines-or-artifacts" tabindex="-1">Triangle Lines Or Artifacts <a class="header-anchor" href="#triangle-lines-or-artifacts" aria-label="Permalink to &quot;Triangle Lines Or Artifacts&quot;">​</a></h2><p>If artifacts follow triangle edges or diagonals, inspect the mesh data.</p><p>Check:</p><ul><li>Degenerate or very thin triangles.</li><li>Zero-area UV triangles.</li><li>Overlapped UV triangles.</li><li>UV islands that are too small for the render target.</li><li>LODs with different triangulation or UV layout.</li><li>Material logic that changes UVs after the paint function.</li></ul><p>When the artifact follows mesh triangulation, changing brush settings usually hides the symptom instead of fixing the source.</p><h2 id="skeletal-mesh-notes" tabindex="-1">Skeletal Mesh Notes <a class="header-anchor" href="#skeletal-mesh-notes" aria-label="Permalink to &quot;Skeletal Mesh Notes&quot;">​</a></h2><p>Check:</p><ul><li>The skeletal mesh has the selected <code>UVChannel</code> on the rendered LOD.</li><li>Collision or the physics asset produces hits near the visible surface.</li><li>The animated pose is not too far from collision.</li><li>LODs have compatible paint UVs.</li><li>Mirrored body parts do not share paint UVs unless mirrored paint is desired.</li></ul><p>The normal skeletal paint path uses GPU screen projection data. Per-poly collision, <code>FindCollisionUV</code>, and Support UV From Hit Results are not required for normal painting.</p><p><code>Max Skeletal Mesh UVFallback Distance</code> only affects lower-level fallback hit resolving paths. It does not control brush size or the normal GPU brush result.</p><h2 id="static-mesh-nanite-and-lod-notes" tabindex="-1">Static Mesh, Nanite, And LOD Notes <a class="header-anchor" href="#static-mesh-nanite-and-lod-notes" aria-label="Permalink to &quot;Static Mesh, Nanite, And LOD Notes&quot;">​</a></h2><p>For static meshes:</p><ul><li>Keep paint UVs available on LOD 0 and any LOD used by the material.</li><li>Keep paint UV layouts compatible across LODs.</li><li>Make sure the visible material section is the section being hit.</li><li>Test a non-Nanite duplicate if a Nanite asset behaves differently during hit or UV resolution.</li></ul><h2 id="material-color-or-material-settings-look-wrong" tabindex="-1">Material Color Or Material Settings Look Wrong <a class="header-anchor" href="#material-color-or-material-settings-look-wrong" aria-label="Permalink to &quot;Material Color Or Material Settings Look Wrong&quot;">​</a></h2><p>If color is too dark, washed out, or black paint does not look black, check the material graph after the paint function.</p><p>Common causes:</p><ul><li>The paint output is multiplied by another texture or tint.</li><li><code>Alpha</code> is lower than expected.</li><li>Vertex color, overlay logic, or another material layer overrides the result.</li><li>Lighting and exposure change how the color appears in the level.</li></ul><p>For metallic and roughness:</p><ul><li>Enable <code>Create Painted Material Settings Render Target</code>.</li><li>Connect the material function metallic and roughness outputs.</li><li>Make sure the material does not override those values after the paint function.</li><li>Remember that <code>Initial Material Settings Color</code> uses red for metallic and green for roughness.</li></ul><p>For the brush preview:</p><ul><li>Connect <code>Brush Emissive</code> to <code>Emissive Color</code>.</li><li>Increase <code>Brush Area Preview Emissive Intensity</code> on the controller if the ring is too dim.</li><li>Keep <code>Brush Area Preview Color</code> white if the preview should follow the current paint color.</li></ul><h2 id="brush-preview-does-not-match-painted-area" tabindex="-1">Brush Preview Does Not Match Painted Area <a class="header-anchor" href="#brush-preview-does-not-match-painted-area" aria-label="Permalink to &quot;Brush Preview Does Not Match Painted Area&quot;">​</a></h2><p>Check:</p><ul><li>Preview and paint resolve the same target component.</li><li><code>BrushSize</code> is changed through <code>SetBrushSize</code> so UI and controller state stay synchronized.</li><li>Material <code>UV Index</code> and target <code>UVChannel</code> match.</li><li>The mesh has consistent paint UV density.</li><li>The mesh does not use mirrored or overlapped paint UVs where independent paint is expected.</li></ul><p>Preview and paint now use the same GPU projection path. If they still appear different, the usual causes are a material reading the wrong UV channel, inconsistent UV texel density, mirrored paint UVs, or a material graph modifying the paint result after the <code>Mesh Painting</code> function.</p><h2 id="asset-preparation-recommendations" tabindex="-1">Asset Preparation Recommendations <a class="header-anchor" href="#asset-preparation-recommendations" aria-label="Permalink to &quot;Asset Preparation Recommendations&quot;">​</a></h2><p>For the most reliable runtime painting:</p><ul><li>Use a dedicated paint UV channel.</li><li>Keep paint UVs unique when independent surface painting is required.</li><li>Avoid mirrored UVs on the paint UV channel unless mirrored painting is desired.</li><li>Keep UV islands inside 0-1.</li><li>Use enough island padding.</li><li>Keep texel density consistent.</li><li>Keep LOD paint UVs compatible.</li><li>Triangulate consistently before export.</li><li>Apply object scale before export.</li><li>Test the model with a simple material before using a complex production material.</li></ul><p>You do not need to change your original art UVs. You can keep them for normal textures and add a separate UV channel only for runtime painting.</p>`,77)])])}const m=a(o,[["render",s]]);export{u as __pageData,m as default};
